# 888-DevOps-candidate-HS


Ansible PlayBook:
Install IIS, including sub features
Copy Index.html file from the linux machine to the windows server to display the page we want.
Add HTTPS binding, using self certificate(Couldn't figure out how to bind the self signed certificate) on port 443
Installing DNS server, including sub tools. Missing: DNS fowared + creating records
